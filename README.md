# Электронный Юрист - Интеллектуальный помощник

Современное веб-приложение для юридических консультаций с ИИ (DeepSeek), включающее систему лимитов, механизм сбора отзывов и панель администратора.

## 🚀 Быстрый запуск (локально)

1. **Клонируйте проект и установите зависимости:**
   ```bash
   git clone https://github.com/stepanboost/Yrist111.git
   cd Yrist111
   npm install
   ```

2. **Настройте переменные окружения:**
   ```bash
   cp .env.example .env
   ```
   Отредактируйте `.env` и укажите URL вашего бэкенда:
   ```
   VITE_API_BASE_URL=http://localhost:8000
   ```

3. **Запустите сервер разработки:**
   ```bash
   npm run dev
   ```

## 🌐 Деплой на Netlify

### Шаг 1: Import from GitHub
1. Зайдите на [Netlify](https://app.netlify.com)
2. Нажмите **"Add new site"** → **"Import an existing project"**
3. Выберите **GitHub** и репозиторий `Yrist111`

### Шаг 2: Настройки сборки
Netlify автоматически определит настройки из `netlify.toml`:
- **Build command:** `npm run build`
- **Publish directory:** `dist`

### Шаг 3: Переменные окружения
1. Перейдите в **Site settings** → **Environment variables**
2. Добавьте переменную:
   | Key | Value |
   |-----|-------|
   | `VITE_API_BASE_URL` | `https://your-backend-api.com` |

### Шаг 4: Деплой
1. Нажмите **"Trigger deploy"** → **"Deploy site"**
2. Дождитесь завершения сборки

> ⚠️ **Важно:** Фронтенд не содержит API ключей. Все запросы к DeepSeek должны проходить через бэкенд, указанный в `VITE_API_BASE_URL`.

## 🛠 Технологический стек

- **Frontend:** React 18, TypeScript, Tailwind CSS, Vite
- **ИИ:** DeepSeek через OpenRouter API (вызов через бэкенд)
- **UI:** Lucide Icons, CSS анимации, адаптивная верстка
- **Экспорт:** Библиотека `docx` для генерации Word документов
- **Хранилище:** LocalStorage API

## 📋 Функционал

- **Чат с ИИ:** 10 бесплатных ходов, затем требование развернутого отзыва (>400 символов)
- **Загрузка документов:** Поддержка PDF и DOCX файлов
- **Экспорт:** Сохранение диалогов в формат .docx
- **История:** Автоматическое именование диалогов, удаление, сохранение контекста
- **Админка:** Мониторинг статистики, управление базой знаний и промптами

## 📁 Структура проекта

```
├── components/        # React компоненты
├── services/          # API сервисы
├── lib/               # Утилиты и store
├── types.ts           # TypeScript типы
├── nastroiks.ts       # Конфигурация модели
├── netlify.toml       # Конфигурация Netlify
└── .env.example       # Пример переменных окружения
```

## 🔒 Безопасность

- API ключи **НЕ** хранятся во фронтенде
- Все запросы к ИИ проходят через защищённый бэкенд
- Переменные окружения с префиксом `VITE_` доступны только публичные данные
